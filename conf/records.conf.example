# Enter (sub)domains with resource numbers you want to point to your own (dynamic) IP-address
# Each (sub)domain and resource number key-value pair on its own separate line
# example.org=12323
# blog.example.org=146532
# example.com=112122
